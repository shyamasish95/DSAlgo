namespace PracticeProj.PracticeCode
{
    public class DecimalToBinary
    {
        /*
            To convert from decimal to binary, We need to use modulo operator(%)
            Eg: Given humber is 234
            int[] arr = new int[];
            int n = 234;
            while(n>0)
            {
                arr.Add(n%2);
                n=n/2;
            }
            Reverse(arr);
        */
    }
}